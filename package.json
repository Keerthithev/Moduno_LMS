{
  "name": "moduno-lms",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd frontend && npm install && npm start",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm install && npm run build && npm run serve",
    "install-deps": "cd frontend && npm install"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  }
}
